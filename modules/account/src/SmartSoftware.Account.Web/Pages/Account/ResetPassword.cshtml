@page
@inject IHtmlLocalizer<AccountResource> L
@inject IHtmlLocalizer<IdentityResource> LI
@using Microsoft.AspNetCore.Mvc.Localization
@using SmartSoftware.Account.Localization
@using SmartSoftware.Identity.Localization
@model SmartSoftware.Account.Web.Pages.Account.ResetPasswordModel
@inject SmartSoftware.AspNetCore.Mvc.UI.Layout.IPageLayout PageLayout
<div class="card mt-3 shadow-sm rounded">
    <div class="card-body p-5">
        <h4>@L["ResetPassword"]</h4>
        @if (Model.InvalidToken)
        {
            <div class="alert alert-danger">
                @LI["SmartSoftware.Identity:InvalidToken"]
            </div>
        }
        else
        {
            <form method="post">
                <p>@L["ResetPassword_Information"]</p>
                <ss-input asp-for="UserId"/>
                <ss-input asp-for="ResetToken"/>
                <ss-input asp-for="Password" autocomplete="new-password" />
                <ss-input asp-for="ConfirmPassword" autocomplete="new-password" />
                <a ss-button="Secondary" asp-page="./Login">@L["Cancel"]</a>
                <ss-button type="submit" button-type="Primary" text="@L["Submit"].Value"/>
            </form>
        }
    </div>
</div>
