@page
@using Microsoft.AspNetCore.Mvc.Localization
@using SmartSoftware.Docs
@using SmartSoftware.Docs.Localization
@using SmartSoftware.Docs.Pages.Documents
@inject IHtmlLocalizer<DocsResource> L
@model IndexModel
@{
}
@section styles {
    <ss-style-bundle name="@typeof(IndexModel).FullName">
        <ss-style src="/Pages/Documents/Shared/Styles/vs.css" />
    </ss-style-bundle>
}
<div class="p-5">

    @if (Model.Projects == null || !Model.Projects.Any())
    {
        <div class="alert alert-secondary text-center" role="alert">
            @L["NoProjectWarning"]
        </div>
    }
    else
    {
        <h1>@L["Projects"]</h1>
        <p>
            <ul style="list-style-type: upper-roman;">
                @foreach (var project in Model.Projects)
                {
                    <li>
                        <h3>
                            <a href="@(Model.DocumentsUrlPrefix + project.ShortName)">
                                @project.Name
                            </a>
                        </h3>
                    </li>
                }
            </ul>
        </p>
    }

</div>
