/* This file is automatically generated by SS framework to use MVC Controllers from javascript. */


// module blogging

(function(){

  // controller smartsoftware.blogging.blogFiles

  (function(){

    ss.utils.createNamespace(window, 'smartsoftware.blogging.blogFiles');

    smartsoftware.blogging.blogFiles.get = function(name, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/files/' + name + '',
        type: 'GET'
      }, ajaxParams));
    };

    smartsoftware.blogging.blogFiles.getFile = function(name, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/files/www/' + name + '',
        type: 'GET'
      }, ajaxParams));
    };

    smartsoftware.blogging.blogFiles.create = function(input, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/files/images/upload' + ss.utils.buildQueryString([{ name: 'name', value: input.name }]) + '',
        type: 'POST'
      }, ajaxParams));
    };

  })();

  // controller smartsoftware.blogging.blogs

  (function(){

    ss.utils.createNamespace(window, 'smartsoftware.blogging.blogs');

    smartsoftware.blogging.blogs.getList = function(ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/blogs',
        type: 'GET'
      }, ajaxParams));
    };

    smartsoftware.blogging.blogs.getByShortName = function(shortName, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/blogs/by-shortname/' + shortName + '',
        type: 'GET'
      }, ajaxParams));
    };

    smartsoftware.blogging.blogs.get = function(id, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/blogs/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller smartsoftware.blogging.comments

  (function(){

    ss.utils.createNamespace(window, 'smartsoftware.blogging.comments');

    smartsoftware.blogging.comments.getHierarchicalListOfPost = function(postId, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/comments/hierarchical/' + postId + '',
        type: 'GET'
      }, ajaxParams));
    };

    smartsoftware.blogging.comments.create = function(input, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/comments',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    smartsoftware.blogging.comments.update = function(id, input, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/comments/' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    smartsoftware.blogging.comments['delete'] = function(id, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/comments/' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

  })();

  // controller smartsoftware.blogging.posts

  (function(){

    ss.utils.createNamespace(window, 'smartsoftware.blogging.posts');

    smartsoftware.blogging.posts.getListByBlogIdAndTagName = function(blogId, tagName, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/posts/' + blogId + '/all' + ss.utils.buildQueryString([{ name: 'tagName', value: tagName }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

    smartsoftware.blogging.posts.getTimeOrderedList = function(blogId, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/posts/' + blogId + '/all/by-time',
        type: 'GET'
      }, ajaxParams));
    };

    smartsoftware.blogging.posts.getForReading = function(input, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/posts/read' + ss.utils.buildQueryString([{ name: 'url', value: input.url }, { name: 'blogId', value: input.blogId }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

    smartsoftware.blogging.posts.get = function(id, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/posts/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    smartsoftware.blogging.posts.create = function(input, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/posts',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    smartsoftware.blogging.posts.update = function(id, input, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/posts/' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    smartsoftware.blogging.posts.getListByUserId = function(userId, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/posts/user/' + userId + '',
        type: 'GET'
      }, ajaxParams));
    };

    smartsoftware.blogging.posts.getLatestBlogPosts = function(blogId, count, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/posts/' + blogId + '/latest/' + count + '',
        type: 'GET'
      }, ajaxParams));
    };

    smartsoftware.blogging.posts['delete'] = function(id, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/posts/' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

  })();

  // controller smartsoftware.blogging.tags

  (function(){

    ss.utils.createNamespace(window, 'smartsoftware.blogging.tags');

    smartsoftware.blogging.tags.getPopularTags = function(blogId, input, ajaxParams) {
      return ss.ajax($.extend(true, {
        url: ss.appPath + 'api/blogging/tags/popular/' + blogId + '' + ss.utils.buildQueryString([{ name: 'resultCount', value: input.resultCount }, { name: 'minimumPostCount', value: input.minimumPostCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

})();


