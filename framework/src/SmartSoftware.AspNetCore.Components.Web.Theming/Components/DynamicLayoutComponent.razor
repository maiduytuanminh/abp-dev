@if (SmartSoftwareDynamicLayoutComponentOptions.Value.Components.Any())
{
    foreach (var (componentType, parameters) in SmartSoftwareDynamicLayoutComponentOptions.Value.Components)
    {
        <DynamicComponent Type="@componentType" Parameters="@parameters" />   
    }
}